/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com'], // Añadir el dominio permitido para imágenes externas
    },
}

export default nextConfig
